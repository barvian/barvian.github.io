{
  "name": "barvian",
  "version": "0.0.0",
  "description": "Personal portfolio.",
  "private": "true",
  "scripts": {
    "gulp": "gulp jekyll-default",
    "build": "gulp jekyll-build",
    "watch": "gulp jekyll-watch",
    "clean": "gulp clean"
  },
  "devDependencies": {
    "bower": "^1.6.5",
    "gulp": "^3.9.0",
    "gulpfile": "mbarvian/gulpfile.js"
  }
}
